# P9 — заключение по нагрузочному тесту

Сформировано: 2026-07-16T12:35:54+00:00 UTC.

| Профиль | HTTP p95 | Dispatch 600 | Пороги |
|---|---:|---|---|
| 2 vCPU / 4 GiB | 9995.0 ms | status=done, orders=600, duration=9.46 s | FAIL |
| 4 vCPU / 8 GiB | 17.7 ms | status=done, orders=600, duration=5.11 s | PASS |

**Минимальная измеренная конфигурация — 4 vCPU / 8 GiB.**

Рекомендация с запасом ×2 — **8 vCPU / 16 GiB**.

Профиль 2/4 не прошёл пользовательские SLO: p95 9995,0 мс и 56,5487%
ненормальных WS-обрывов. Быстрый dispatch (9,46 с) и низкая доля HTTP-ошибок
не компенсируют накопление очереди API. Профиль 4/8 прошёл все thresholds без
ошибок и обрывов, а dispatch не поднял общий p95 выше лимита.

Вывод основан только на `loadtest/results/*.md`; при изменении кода, базы, Docker/ядра или железа матрицу следует повторить.
