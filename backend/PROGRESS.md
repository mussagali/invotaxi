# PROGRESS

Регламент: [03-ORCHESTRATION.md](03-ORCHESTRATION.md).

**Регламент по этапам:**
- P0–P2: Codex CLI отсутствовал в PATH — Fable писал код сам
  (обе роли), цикл этапа и gates соблюдались.
- С P3 (указание пользователя 2026-07-15): весь код приложения пишет
  Codex (`codex exec`, модель gpt-5.6-sol, reasoning medium). Fable
  формирует промты, запускает тесты, ревьюит, чинит мелочи при
  необходимости; Edit/Write — только для служебных файлов.

| Этап | Статус | Коммит | Примечания |
|---|---|---|---|
| P0 каркас | ✅ принят 2026-07-15 | P0 | 4 контейнера healthy; health 200/503 проверен руками; alembic в контейнере ok; ruff+mypy+pytest (7 тестов) зелёные. `make` на Windows-хосте нет — цели Makefile проверены прямыми командами, в CI — те же команды |
| P1 БД | ✅ принят 2026-07-15 | P1 | 9 таблиц + 6 enum + партиционированный track_history; up/down/up проверяется фикстурой на каждом прогоне тестов; сид идемпотентен (82 users / 150 orders, 2 twin-пары с разными клиентами); argon2-cffi добавлен на этап раньше (P2 требует argon2 — сиды сразу пишут боевой формат хэша) |
| P2 auth | ✅ принят 2026-07-15 | P2 | argon2 + JWT (access 15м/refresh 30д, ротация, детект кражи → revoke all), rate-limit 5/мин (429+Retry-After проверен руками), RBAC deps; 32 теста зелёные; в логах телефон маскирован, пароля нет. Попутный фикс P0: обработчик ошибок терял exc.headers (Retry-After/WWW-Authenticate) |
| P3 orders | ✅ принят 2026-07-15 | P3 | первый этап, написанный Codex (gpt-5.6-sol). CRUD+SM+twins+импорт; 39 тестов зелёные; grep: order.status пишется только в order_state.py; ручные негативы: чужой заказ 404, created→completed 409, transition клиентом 403, cancel без reason 422, событие в order_events с reason/cancelled_by. Ревью-замечаний уровня FIX не было |
| P4 drivers/GPS | ✅ принят 2026-07-15 | P4 | Codex, 1 итерация FIX (блокер: flush читал ARQ-redis без decode_responses — в проде терял бы всю историю; фикс + регрессионный тест на байтовом клиенте). Ингест: 1 Lua-вызов (rate-limit+GEOADD+HSET+TTL 120с+аномалии), 44 теста зелёные. Бенч: последовательно p95=10.4мс (<20мс DoD); под 100 параллельными на 1 dev-воркере p95≈260мс — очередь, не путь (прод-профиль проверяется в P9) |
| P5 routing | ✅ принят 2026-07-15 | P5 | Codex, 1 итерация FIX (датасет не нагружал вместимость — сравнение с legacy было беззубым). Порт 1-в-1 + B1–B7, без pandas, mypy strict, покрытие 97%, 600 заказов за ~18с (<60с). Сравнение на 250: fixed 247/3/0 нарушений vs legacy 250/0/2 нарушения (два 6-местных юнита в седанах) — фиксы работают. Мелкий фикс Fable: перф-гейт не проверяется под coverage-трассировкой (замедление ~4x не относится к гейту). Исходники движка: _reference/ (gitignored) |
| P6 dispatch | ✅ принят 2026-07-15 | P6 | Codex, 2 итерации FIX (1: не написал тесты этапа — 8 сценариев DoD добавлены; 2: job забирал заказы даты без учёта района — двойная обработка между районами, закрыто NOT EXISTS-фильтром + регрессионный тест). Advisory lock на (date,district), упавший job не трогает заказы, близнецы схлопываются в адаптере. Решения: n_rental=0 (виртуальная аренда не проходит FK на drivers — не влезшие идут в exception-список), «район заказа» = район плана, в который он уже попал. Ручной цикл: job 202 → draft → publish → 43 assigned/107 exception на сидах; клиент на /dispatch/jobs → 403 |
| P7 realtime | ✅ принят 2026-07-15 | P7 | Codex, 2 итерации FIX (1: тест бил в несуществующий URL + боевой баг — hub ронял рассылку ClosedResourceError при отправке в закрытое соединение; 2: диспетчеру уходил route.published вместо plan.published + P6-тест обновлён под versioned envelope без ослабления). WS c auth до accept, лимит 3 соединения, EventBus, FCM/Noop push, позиции клиентам с throttle. Smoke: 3×500 WS по 60с, RSS api 127→153→165→168 MiB (плато, утечки нет). httpx переехал в prod-депсы (FCM) — образ пересобран. Известная мелочь: ws_smoke.py печатает клиентский RSS как 0.0 на Windows (косметика) |
| P8 reports | ✅ принят 2026-07-15 | P8 | Codex, 2 итерации FIX (1: не было интеграционных тестов DoD — добавлены 5; 2: PermissionError на named volume /data/reports (root vs appuser) — chown в Dockerfile, + ошибка генерации теперь сохраняется в метаданных отчёта). 3 типа xlsx (dispatcher_sheet/day_summary/unassigned_heatmap), метаданные в Redis TTL 31д, ретенция-cron 30д, stats-эндпойнты. Ручной цикл на стеке: заказ → done → xlsx открывается, только заявленное время. 63 теста зелёные |
| P9 loadtest | ✅ принят 2026-07-16 | P9 | Метрики `/metrics`, Prometheus/Grafana/cAdvisor/PG/Redis exporters, dashboard и 4 alert rules. Два чистых 30-минутных прогона на актуальном коде: 2/4 FAIL (p95 9995мс, WS 56,55%, dispatch 9,46с), 4/8 PASS (p95 17,7мс, errors/WS 0%, dispatch 5,11с). Измеренный минимум 4 vCPU / 8 ГБ, рекомендация ×2 — 8/16. Perf-изменения разнесены по коммитам с before/after |
| P10 deploy | — | — | |

## Журнал решений

- 2026-07-15: git init выполнен в `backend/` (репо = папка с планом,
  как в README плана). Docker Desktop запускался вручную дирижёром.
- 2026-07-15: P0 — тест «Postgres остановлен» реализован как
  DATABASE_URL на закрытый порт (эквивалентно остановленному
  инстансу на уровне TCP), чтобы не гонять stop/start контейнера
  в тестовой сессии.
